apiVersion: v1
kind: Service
metadata:
  name: smartscaler-controller
  namespace: default
  labels:
    app: smartscaler-controller
spec:
  selector:
    app: smartscaler-controller
  ports:
    - name: metrics
      port: 8000
      targetPort: 8000
      protocol: TCP